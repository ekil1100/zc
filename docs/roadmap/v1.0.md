# zc v1.0 roadmap

> Public roadmap snapshot. Detailed agent working notes live in `.agents/zc-v1.0-roadmap.md`.

## Status

zc **尚不适合立即标记 `v1.0.0`**。本地 Zig 0.16 构建与测试通过，但仍有 release blocker。除 outbound 声明对齐外，可靠持久代理选择和本地托管配置导入现已进入内部基础实施；两项用户可见行为目前都尚未实现。

2026-07-15 本地验证：

```bash
zig version
# 0.16.0

env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build test --summary all
# 662/663 tests passed (1 skipped; includes typed StateAuthority, exact downloaded/override writers, catalog coordination, immutable config, migration, exact loader, and runtime descriptor tests)

env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build -Doptimize=ReleaseFast --summary all
# 4/4 steps succeeded
```

本次已验证内部 typed authority、ConfigBundle、exact catalog、RevisionStore、legacy bootstrap/mirror、exact loader 与 runtime descriptor 测试及 ReleaseFast；未重跑完整 migrator/install/full-validation，这些命令仍属于 P0-7。

## v1.0 definition

zc v1.0 is a CLI-first proxy runtime with:

- default mixed inbound;
- daemon lifecycle commands;
- explicit local-dev port override;
- AnyTLS TCP 出站;
- core rule matching;
- rule-provider expansion;
- minimal API;
- install/verify/upgrade/rollback validation scripts.

v1.0 is **not** a full mihomo/clash replacement.

## Release blockers

### P0-1: Toolchain and release workflow alignment — done

当前结果：

- 项目策略要求 Zig 0.16.0+；
- 本地验证使用 Zig 0.16.0；
- CI 与 release workflow 使用 Zig 0.16.0；
- package minimum Zig version 为 0.16.0。

DoD:

```bash
rg --hidden -n '0[.]15[.]2|0[.]15[.]0|0[.]15[.]x' .github build.zig.zon AGENTS.md README.md docs || true
env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build test --summary all
env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build -Doptimize=ReleaseFast --summary all
bash scripts/run-full-validation.sh
```

### P0-2: Outbound protocol declaration alignment

Config currently accepts some proxy types that are not implemented by outbound runtime.

Decision required before GA:

- implement HTTP/SOCKS5 outbound, or
- mark them unsupported in validator/docs/migrator so users fail early.

Recommended v1.0 choice: mark unsupported and defer implementation.

### P0-3: `zc test --json` stdout contract — done

`zc test --json` success output emits valid JSON on stdout. Remaining GA validation should keep this covered by smoke tests, including failure paths where actionable JSON errors are expected.

Follow-up (done): the full CLI output-contract alignment landed — every command emits a uniform `{"ok","command","data"|"error"}` envelope on stdout, exit codes are uniform (0/1/2), help is generated per command, `zc reload`/`zc up`/`zc down`/`zc version`/`--foreground`/`--no-color` exist, and `test`/`doctor` failures report `CHECKS_FAILED` with non-zero exit. Contract: [`docs/cli/spec.md`](../cli/spec.md); acceptance record: [`docs/cli/ux-workflow.md`](../cli/ux-workflow.md). This closes the CLI-contract work item only; the other GA gates in this roadmap (notably P0-2 outbound policy and the final smoke gate) remain open.

### P0-4: Remove TUI from v1.0 — done

TUI 已从 v1.0 命令表面、默认构建和 active docs 移除。已完成的清理包括：

- remove `zc tui` from CLI help and dispatch;
- stop compiling `src/tui.zig` in the default build;
- remove TUI user documentation;
- keep any historical TUI notes only under `docs/archive/`.

### P0-5: Documentation cleanup — done

根目录 `ROADMAP.md` 与 `TASKS.md` 已删除或归档。当前文档入口为：

- `README.md` at repo root;
- `docs/README.md` as documentation index;
- this roadmap as public v1.0 plan;
- `.agents/zc-v1.0-roadmap.md` as agent working notes.

All stale claims such as GA-ready status, full API v1, WebSocket event stream, or TUI support must be removed from public docs or moved to archive.

### P0-6: Reliable proxy selection and local config import — in progress

当前代理选择可能让持久 metadata 与 daemon runtime 状态分裂。v1.0 计划要求：

- revisioned managed-config identity；
- 使用单调 generation 的 durable desired selection；
- daemon 接收流量前先完成启动对账；
- 明确区分 desired/runtime 与 selected/resolved；
- 提供 CLI-only `zc config import <path>`，导入经过验证的本地配置 bundle；
- 只有 TCP、UDP 和多路复用逻辑 flow 都具备安全追踪与取消后，才开放可选连接关闭；
- 通过 crash recovery 与 ReleaseFast 性能门禁。

内部 transactional/typed authority、publish/commit/mirror coordinator、shadow-only ConfigBundle/resolver、exact schema-2 catalog、immutable RevisionStore、legacy bootstrap/可重建 mirror，以及 exact loader/tracked runtime descriptor seam 已完成；它们尚未接入 daemon、CLI、API 或现有 managed reader/writer 生产路径。下一步先在 Batch 5 将全部 managed writer 同批切到 Authority，避免 partial cutover split-brain；随后完成 daemon exact identity、actual controller endpoint、CLI tracked discovery/handshake 与启动 generation reconcile。`config import` 默认只保存、不激活、不应用仍是 roadmap 契约，不代表当前命令已实现；当前 CLI 契约仍以 [`docs/cli/spec.md`](../cli/spec.md) 为准。

## P0-7: Final smoke gate

Before tagging:

```bash
git status --short --branch
env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build test --summary all
env ZIG_GLOBAL_CACHE_DIR=/tmp/zig-cache zig build -Doptimize=ReleaseFast --summary all
bash tools/config-migrator/run-all.sh
bash scripts/install/run-all-regression.sh
bash scripts/run-full-validation.sh
./zig-out/bin/zc start --port 29001 -c testdata/config/minimal.yaml --json
./zig-out/bin/zc status --json
./zig-out/bin/zc stop --json
```

只有 P0-2、P0-6 完成且本 gate 通过后，才允许标记 `v1.0.0`。
